[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "footing-prelim"
version = "0.1.0"
description = "Preliminary isolated spread footing sizing assistant"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
